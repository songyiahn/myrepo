---
title: "hw5"
format: html
---

```{r}
library(tidyverse)
```

```{r}
gss_cat %>%
  ggplot(aes(tvhours, partyid)) +
  geom_col()
```
```{r}
ggplot(mpg, aes(x = displ, y = hwy)) +
  geom_point()
```

